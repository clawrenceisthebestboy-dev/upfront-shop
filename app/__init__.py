"""Up Front Shop — package marker."""

# Bump this every release. The auto-updater compares the shop laptop's
# installed APP_VERSION against the `version` field in the remote
# manifest (latest.json) to decide whether to prompt for an update.
APP_VERSION = "1.4.0"
